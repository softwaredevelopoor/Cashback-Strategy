import * as anchor from "@coral-xyz/anchor";

module.exports = async function deploy(_provider: anchor.AnchorProvider) {
  // Anchor migration hook. Initialization is done explicitly from scripts/tests.
};
